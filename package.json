{
  "name": "resenha-backend",
  "version": "1.0.0",
  "description": "Backend dedicado da plataforma Resenha — REST (pareamento do Resenha Client) + WebSocket server + gerenciamento de partidas",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch --env-file=.env src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "ws": "^8.18.0"
  }
}
